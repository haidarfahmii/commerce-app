import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.pinimg.com",
      "cultdesign.com.au",
      "tripuracraftco.in",
      "i.ebayimg.com",
    ],
  },
};

export default nextConfig;
