import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.pinimg.com",
      "tripuracraftco.in",
      "cultdesign.com.au",
      "i.ebayimg.com",
    ],
  },
};

export default nextConfig;
